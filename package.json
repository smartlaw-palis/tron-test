{
  "name": "liquidre-contract",
  "version": "1.0.0",
  "dependencies": {
    "babel-runtime": "6.26.0",
    "ethers": "^3.0.26",
    "stringstream": "0.0.6",
    "tronweb": "^1.0.9"
  },
  "scripts": {
    "compile": "rm -rf build && tronbox compile",
    "test": "rm -rf build && tronbox compile && node tests.js"
  }
}
